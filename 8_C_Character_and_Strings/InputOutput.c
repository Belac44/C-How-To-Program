#include <stdio.h>
#include <string.h>


int main( void ){
    char c = 'b';
    int x, y,z;
    double d,e,f;
    char *ptr;
    char s1[100];
    char s2[100];

    puts("Enter a letter: =>");
    // gets(s1);
    // puts(s1);
    // ptr = strchr(s1,c);
    // putchar(c);
    
}